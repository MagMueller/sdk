---
title: Run a task
description: "Give a high-accuracy browser agent a goal and get the result."
icon: rocket
---

Create an [API key](https://cloud.browser-use.com/settings?tab=api-keys&new=1) and export it:

```bash
export BROWSER_USE_API_KEY=your_key
```

<CodeGroup>
```python Python
from browser_use_sdk.v4 import BrowserUse

client = BrowserUse()
run = client.runs.create("Find the top Hacker News story")
run = client.runs.wait_for_completion(run.id)
print(run.result)
```
```typescript TypeScript
import { BrowserUse } from "browser-use-sdk/v4";

const client = new BrowserUse();
const run = await client.runs.create({
  task: "Find the top Hacker News story",
});
const result = await client.runs.waitForCompletion(run.id);
console.log(result.result);
```
```bash curl
curl https://api.browser-use.com/api/v4/runs \
  -H "X-Browser-Use-API-Key: $BROWSER_USE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"task":"Find the top Hacker News story"}'
```
</CodeGroup>

Install the SDK with `pip install browser-use-sdk` or
`npm install browser-use-sdk`. Curl needs no installation.

Every new run implicitly creates a **session** and a **workspace**:

<Frame>
  <img
    src="/cloud/images/v4-agent-overview.png"
    alt="A run belongs to a conversation session and uses a persistent workspace"
  />
</Frame>

<CardGroup cols={3}>
  <Card title="Sessions" icon="comments" href="/cloud/agent/sessions">
    Continue the same conversation and browser.
  </Card>
  <Card title="Workspaces" icon="folder" href="/cloud/agent/workspaces">
    Keep files across runs and sessions.
  </Card>
  <Card title="Observability" icon="chart-line" href="/cloud/agent/observability">
    Poll ordered events while a run is active.
  </Card>
</CardGroup>

<Card title="Copy the API V4 docs for an LLM" icon="sparkles" href="/cloud/llms.txt">
  Give this compact context file to your coding agent.
</Card>
